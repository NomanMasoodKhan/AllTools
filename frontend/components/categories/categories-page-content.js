import PageShell from '../layout/page-shell';

export default function CategoriesPageContent() {
  return (
    <PageShell title="Categories">
      <p>Category index for CyberMart MVP.</p>
      <p>Each tool must belong to at least one category.</p>
    </PageShell>
  );
}
